

import { Button } from "./ui/button"

const Submission = () => {
  return (
    <div className="mt-8 flex justify-between">
        <Button variant="wudestructive">Cancel</Button>
        <Button variant="wuapprove">Pre-print Receipt</Button>
    </div>
  )
}

export default Submission